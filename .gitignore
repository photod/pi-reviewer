# Dev-only notes & evidence — NEVER publish. Lives in docs/, out of the public tree.
# NOTE: `docs` is a symlink (docs -> ../pi-reviewer-notes) — no trailing slash, or git
# won't match the symlink itself (a trailing slash only matches a real directory).
docs

# Session worklog — dev-only, purged from public history (2026-07-25). Kept at repo root
# (not under docs/) so the `./handoff.md` session-start convention still finds it locally.
handoff.md

# OS / editor cruft
.DS_Store
**/.DS_Store

# Python bytecode
__pycache__/

# Redacted review snapshots (pi-stage.sh) — masked copies of your code; never publish.
.pi-review/
